# Intro

This repository provides a React starter template for creating a documentation website. You can easily customize it by modifying the URLs in `files.json` or `config.js` to point to your own Markdown and PDF files, and the site will display your content.

<!-- This is a comment -->
